import { ProSearchPipe } from './pro-search.pipe';

describe('ProSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ProSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
