import { VenSearchPipe } from './ven-search.pipe';

describe('VenSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new VenSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
